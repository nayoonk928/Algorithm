<?php
    $input = trim(fgets(STDIN));
    
    // 공백을 기준으로 문자열을 분리하여 배열에 저장
    list($n, $m) = explode(' ', $input);
    
    // 문자열을 정수로 변환 
    $n = intval($n);
    $m = intval($m);
    
    echo $n * $m;
?>
